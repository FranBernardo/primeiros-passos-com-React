import React from "react";
import Logo from "./code.png";
import "./style.css";

const Header = () => (
  <header id="cabeçalho">
    <img src={Logo} />
  </header>
);

export default Header;
